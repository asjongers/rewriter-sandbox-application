{{!-- Page to create a new subject --}}
<div class="new">
  <h2 class="title">{{t (humanize 'New subject')}}</h2>

  {{!--
    Actions to save new subject
    or delete new subject.
  --}}
  <div class="actions">
    <button type="button" class="btn save" title="Save new subject" {{action 'save' model}}>Save</button>
    <button type="button" class="btn cancel" title="Cancel" {{action 'cancel' model}}>Cancel</button>
  </div>

  {{!-- Table to show details of subject. --}}
  <table>
    <thead>
      <tr>
        <th>Attribute</th>
        <th>Data</th>
      </tr>
    </thead>
    <tbody>
      {{!-- Creating rows for attributes depending on the type of property --}}
            <tr>
        <td>name</td>
<td align='middle'>{{handle-attribute model=model attribute='name' editable=true}}</td>
      </tr>
      
      <tr><td>classes</td><td align='middle'>{{edit-has-many relName=model.classes relType="class"}}</td></tr>
    </tbody>
  </table>
</div>
