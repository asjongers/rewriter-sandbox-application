{{!-- Basic view with navbar, menu on the left and content on the right. --}}
<div class="navbar">
  {{#link-to "index" tagName="h1" class="title" }}
    Test Application
  {{/link-to}}
  <div class="navigation-menu">
    {{#power-select options=options selected=selectedOption onchange=(action "setOption") as |option|}}
      {{option.name}}
    {{/power-select}}
    {{navigation-menu}}
  </div>
</div>
<div class="main-content">
  {{outlet}}
</div>
